from setuptools import setup, find_packages

setup(
    name='pymyo',
    version='0.1',
    packages=find_packages(),
    install_requires=[
    ],
    tests_require=[],
    dependency_links=[
    ],
)
