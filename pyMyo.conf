[Config]
##System shell to invoke
shell   = /bin/bash

##The shell prompt to show
prompt  = pyMyo

##Whether to use ipython console by default if available (via the '>' command)
use_ipy = True

##Whether to show the startup pyMyo banner
banner  = True

#Print debugging inf on error ?
debug   = False
